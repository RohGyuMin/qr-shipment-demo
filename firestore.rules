rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ 데모 전용 — 누구나 읽기/쓰기 가능. 운영 전 반드시 인증/권한 규칙으로 교체할 것.
    match /shipments/{doc} {
      allow read, write: if true;
    }
  }
}
